"""Example model implementations for spectrax.

This package contains production-style transformer architectures
(Llama 3, Qwen 2.5) that are reused across the ``examples/`` and
``benchmarks/`` directories. Each model is written **once** and runs
under both SPMD and MPMD via :func:`spectrax.run`.
"""
